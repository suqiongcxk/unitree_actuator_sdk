# Mesh policy

The portable model intentionally uses analytic visual and collision primitives. This avoids licensing ambiguity and mesh-unit/contact instability while preserving the audited URDF joint origins, masses, inertias and link lengths. No parent-repository mesh is required.
