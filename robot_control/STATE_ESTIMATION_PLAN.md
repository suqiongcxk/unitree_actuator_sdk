# 四足机器人状态估计完善计划

本文档用于逐步完善状态估计链路。每一步都必须先完成代码实现、离线/干运行测试和实机静态验证，再进入下一步；在 NN 观测定义尚未与训练代码完全一致前，不允许驱动真实电机运行 NN 策略。

## 当前基线

- JY901S 使用 I2C1_M4，六轴融合模式（加速度计＋陀螺仪）。
- IMU 与机体采用右手坐标系：X 向前、Y 向左、Z 向上。
- 实机方向已确认：左侧下沉 Roll 为负，头部下沉 Pitch 为正，向左转 Yaw 为正。
- 四元数寄存器和缩放已修正，静止/运动时模长约为 1。
- `PassthroughEstimator` 目前仅直通 IMU、电机反馈；位置和线速度固定为零，接触状态固定为 false。
- 当前 48 维 NN 观测布局仍是占位定义，必须以训练代码为最终依据。

---

## Step 1：四元数归一化、方向和失效检查

### 工作内容

- 检查四元数四个分量是否为有限值（拒绝 NaN/Inf）。
- 检查原始模长是否处于合理范围，随后归一化。
- 处理相邻帧 `q` 与 `-q` 的符号连续性。
- 确认 JY901S 输出是 `body → world` 还是 `world → body`。
- 增加 IMU 数据超时、连续读取失败和无效姿态保护。

### 验收标准

- 水平和缓慢转动时，归一化后 `|q|` 接近 1。
- 静止时四元数无随机符号翻转。
- 拔掉 IMU 或注入非法值时，状态被标记为无效，NN 不再下发新动作。

- [x] 已完成（代码、纯软件测试、实机姿态方向及 IMU 断连失效验证均通过）

## Step 2：陀螺仪静态零偏校准与低通滤波

### 工作内容

- 上电静止采样，计算 X/Y/Z 三轴零偏。
- 校准阶段检测机器人是否确实静止，运动时拒绝保存零偏。
- 原始角速度减去零偏后进行低延迟低通滤波。
- 增加异常突跳、量程和有限值检查。
- 保持单位为 `rad/s`，坐标系为机体坐标系。

### 验收标准

- 静止时三轴角速度长期接近零。
- 缓慢转动时方向正确、响应连续，无明显过度延迟。
- 校准期间移动 IMU 能被检测并报告失败。

- [x] 已完成（代码、纯软件测试、实机静态零偏、运动拒绝校准及三轴低通响应均通过）

## Step 3：投影重力方向实机验证

### 工作内容

- 使用归一化四元数计算机体坐标系下的单位重力方向。
- 根据 Step 1 的四元数方向决定是否使用共轭。
- 将投影重力计算从 NN 输入拼装中移到状态估计层，避免重复或不一致实现。
- 检查投影重力有限性和模长。

### 验收标准

- 水平时投影重力约为 `[0, 0, -1]`（必须与训练环境一致）。
- 左右、前后倾斜时各分量变化方向与训练环境一致。
- 投影重力模长稳定接近 1，Yaw 转动不会显著改变它。

- [x] 已完成（纯软件测试和 IMU 实机方向测试均通过）

## Step 4：关节位置、速度顺序及正负号验证

### 工作内容

- 确认 12 个状态下标与 motor ID、腿序、训练关节序完全一致。
- 将编码器角度统一转换为 URDF/训练坐标系关节角。
- 对位置和速度应用相同的减速比与方向映射。
- 检查零点偏置、单位、错误码和反馈时间戳。
- 编写逐关节离线/低风险验证输出。

### 验收标准

- 单独转动任一关节时，只有预期观测下标发生主要变化。
- 关节朝 URDF 正方向运动时，位置和速度均为正。
- 默认站立姿态与训练环境中的默认关节位置一致。

- [ ] 代码审计与纯软件测试已完成；等待逐关节低风险实测

## Step 5：时间戳和状态有效性保护

### 工作内容

- 为 IMU、电机反馈和估计输出维护单调时钟时间戳。
- 正确计算并限制估计周期 `dt`。
- 增加 IMU 数据年龄、电机数据年龄、连续失败计数和整体 `valid` 状态。
- 定义超时阈值和降级策略。
- 无效或过期状态不得进入新的 NN 推理/电机指令下发。

### 验收标准

- 模拟 IMU 或任一电机反馈中断时，能在设定时间内检测。
- 时间戳倒退、异常 `dt` 和部分反馈缺失不会产生异常控制指令。
- 故障时统一转入已定义的安全保持或阻尼退出流程。

- [x] 已完成（纯软件测试、正常实机链路、电机反馈超时、IMU 数据流中断和 Ctrl+C 阻尼退出均通过）

## Step 6：修正 NN 的“上一帧动作”观测

### 工作内容

- 明确训练代码中的 action 定义、缩放和裁剪方式。
- 保存真正的上一帧 NN 原始动作或缩放后动作，与训练定义保持一致。
- 首帧使用训练时约定的初始化值。
- 禁止继续用 `default_pose` 冒充上一帧动作。
- 对比 ONNX 输入维度、顺序、归一化和 clipping 参数。

### 验收标准

- 逐元素对比同一输入在训练端和 C++ 端生成的 observation。
- 48 维观测的每个下标均有明确来源、单位、缩放和范围。
- 离线 ONNX 输出与训练端推理结果在允许误差内一致。

- [x] 已完成（训练端黄金向量逐元素对拍及 1483 帧实机 monitor-only 运行时日志验收均通过）

## Step 7：接触检测与机体线速度估计

### 工作内容

- 基于关节运动学、关节速度和电机力矩建立初始接触判定。
- 条件允许时接入足端力传感器，提高接触判断可靠性。
- 对支撑腿使用足端近似零世界速度约束，反推机体速度。
- 融合 IMU 加速度、腿部运动学和多足接触置信度。
- 估计机体高度，并识别打滑、腾空和落地冲击。

### 验收标准

- 静止站立时估计线速度接近零且不持续漂移。
- 抬起单腿时接触状态切换正确，不污染支撑腿速度约束。
- 手动缓慢平移机体时速度方向正确且连续。
- 打滑或无可靠支撑足时降低估计置信度，不输出虚假高可信速度。

- [x] 已完成（代码、纯软件测试、接触阈值、单/双腿卸载、速度方向、腾空及打滑降置信实机验收均通过）

---

## NN 上机前总门槛

- [ ] 已取得并核对训练端 observation/action 的准确结构。
- [ ] Step 1～Step 6 全部通过；需要线速度的模型还必须通过 Step 7。
- [ ] C++ 与训练端观测逐元素对比通过。
- [ ] ONNX 离线回放和输出安全验证通过。
- [ ] 干运行模式下持续测试，无 NaN、超时、越界或关节顺序错误。
- [ ] 急停、通信故障和状态无效均能阻止 NN 指令覆盖阻尼指令。

## 实施记录

每完成一步，在对应复选框标记完成，并在这里记录日期、修改文件、测试命令、测试结果和遗留问题。

### 2026-08-09 — Step 1 实现

- 修改：`shared_data.h`、`state_estimator.h/.cpp`、`robot_controller.cpp`。
- 新增：`test_state_estimator.cpp` 纯软件测试目标。
- 已实现：有限值检查、模长范围检查、归一化、`q/-q` 连续性、100 ms 超时检查、I2C 失败传播。
- 安全行为：无效状态不进入 NN 推理；连续 3 个估计周期无效后请求统一急停。
- 软件测试：`test_state_estimator` 与 `test_nn_validation` 均通过。
- 结论：Step 1 已验收完成。
- 后续集成测试：实机静态姿态连续性，以及运行中断开 IMU 后是否按预期进入安全退出。

### 2026-08-20 — Step 1 实机验收

- 水平静止：`RPY=(0.709,0.681,1.714) deg`，`g_body=(0.012,-0.012,-1.000)`。
- 头部下沉约 33.2°：`g_body=(0.548,-0.041,-0.836)`；头部抬高约 24.5°：`g_body=(-0.415,-0.018,-0.910)`。
- 左侧下沉约 23.4°：`g_body=(0.011,0.397,-0.918)`；左侧抬高约 24.6°：`g_body=(0.002,-0.417,-0.909)`。
- 水平偏航约 84.3° 后 `g_body=(0.010,-0.035,-0.999)`，确认纯 Yaw 不改变重力投影。
- 运行中断开 JY901S 后立即停止有效姿态输出，持续报告 `status=1 (IMU_READ_FAILED)`，程序未崩溃且保持可退出。
- 修正 `test_projected_gravity` 的诊断优先级，避免把 IMU 读取失败误报成陀螺仪校准中；纯软件回归测试通过。
- 结论：Step 1 实机验收完成。整机控制状态下的断连阻尼与线程回收仍属于系统级安全验收，不影响 Step 1 传感器失效检测结论。

### 2026-08-13 — Step 2 实现

- 保留 JY901S 内部六轴融合和硬件滤波，软件侧只补偿上电残余常值零偏。
- 电机总线启动前采集 50 个连续静止样本（约 1 秒@50 Hz）；检测到运动或加速度模长异常时清空本轮采样。
- 使用三轴在线均值/方差检查校准稳定性，禁止将运动数据保存为零偏。
- 校准后减去零偏，并使用 15 Hz 一阶低通滤波；系数根据真实 IMU 时间戳计算。
- 增加 NaN/Inf、±35 rad/s 量程和相邻帧 10 rad/s 突跳检查。
- 启动流程在校准成功前不启动电机总线；3 秒内未获得合格静止数据则安全终止启动。
- 纯软件测试覆盖：零偏估计、运动重置、低通阶跃、NaN、超量程和突跳拒绝，全部通过。
- 待验收：保持机身静止启动，记录三轴 bias、静止噪声和手动缓慢转动响应。

### 2026-08-20 — Step 2 实机验收

- 新增 `test_gyro_calibration` 专用诊断程序，仅访问 JY901S I2C，不初始化或驱动电机。
- 启动后持续移动 IMU 超过 3 秒时始终不完成校准，确认运动会拒绝/重置静止样本。
- 随后保持静止可正常完成 50 个样本的零偏校准。
- 静止补偿后平均角速度约为 `(-0.00015,-0.00013,-0.00009) rad/s`，各轴 1 秒窗口标准差接近零，显著低于 `0.02 rad/s` 验收阈值。
- 左侧抬高时 X 轴为正，典型原始/滤波均值约 `0.21161/0.21118 rad/s`。
- 头部下沉时 Y 轴为正，典型原始/滤波均值约 `0.16372/0.16379 rad/s`。
- 向左偏航时 Z 轴为正，典型原始/滤波均值约 `0.44871/0.45029 rad/s`。
- 三轴低通输出方向正确、连续且紧密跟随补偿后原始值，未见 NaN、Inf、异常跳变或明显过度延迟。
- 结论：Step 2 实机验收完成。

### 2026-08-13 — Step 3 实现

- 在 `EstimatedState` 中新增机体系单位投影重力及有效标志。
- 统一按 JY901S `body→world` 四元数计算 `R^T × [0,0,-1]`；水平期望为 `[0,0,-1]`。
- NN 观测改为直接读取状态估计结果，删除 NN 层重复的四元数旋转公式。
- 增加投影重力有限性、模长检查和归一化；无效结果不能进入 NN。
- 纯软件测试覆盖：水平、头部下沉 90°、左侧下沉 90°和纯 Yaw，全部通过。
- 新增 `test_projected_gravity` 实机诊断程序，仅访问 I2C，不访问电机。
- 实机结果：头部下沉时 `gx>0`，头部抬高时 `gx<0`；左侧下沉时 `gy>0`，左侧抬高时 `gy<0`。
- 水平静态实测 `g_body=(0.017,-0.014,-1.000)`；顺时针改变 Yaw 后为 `(0.014,-0.016,-1.000)`，验证纯 Yaw 不影响投影重力。
- 结论：JY901S 四元数按 `body→world` 使用及 `R^T × [0,0,-1]` 计算方向均已确认，Step 3 验收完成。

### 2026-08-13 — Step 4 实现

- 确认状态数组严格以 motor ID 为下标，顺序为 Hip `[0..3]`、Thigh `[4..7]`、Calf `[8..11]`。
- 确认位置和速度由电机库先转换到减速器输出端，再用标定零点和方向转换到 URDF 坐标系。
- 增加统一的电机/URDF 力矩方向转换函数。
- 修正 GO-M8010-6 力矩量纲：协议已使用关节输出 N·m，不再额外乘除减速比。
- 将每个电机的 CRC `correct`、`merror` 和数值有限性纳入状态有效性；任何一个关节无效时整帧不得进入 NN。
- 纯软件测试覆盖 12 个唯一标记的排列、ID 完整唯一性，以及位置/速度/力矩双向映射，全部通过。
- 待验收：逐关节低风险运动，确认对应下标、URDF 正方向和默认站立角；力矩用已知载荷单独标定。

### 2026-08-26 — Step 4 左前腿实机验收（部分完成）

- `/dev/ttyS6` 上 ID 0/4/8 以 500 Hz 联合轮询，丢包率分别为 `0.044%`、`0.333%`、`1.110%`，总线循环稳定在 500 Hz；ID 8 保留为后续观察项。
- 逐关节低阻尼手动验证确认 `ID 0 -> FL_hip`、`ID 4 -> FL_thigh`、`ID 8 -> FL_calf`，三者的 `motor_direction=+1` 与实机 URDF 正方向一致。
- 位置 `delta_urdf` 与速度 `dq_urdf` 在正反向运动中符号一致，停止后速度回到零附近；ID 8 往返后位置残差约 `-0.0008 rad`。
- 修复 `ParallelBus::getState()` 在首个有效反馈到达前读出厂商 `MotorData` 未初始化数值的问题；现在首帧反馈前返回零值且保持无效标志，诊断程序和主程序均编译通过。
- Step 4 尚未整体完成：还需验证 FR/RL/RR 共9个关节、重新机械限位标定后的默认站立角，以及已知载荷下的力矩量纲验收。

### 2026-08-26 — Step 4 右前腿实机验收（部分完成）

- `/dev/ttyS4` 上 ID 1/5/9 以 500 Hz 联合轮询，丢包率分别为 `0.822%`、`0.200%`、`0.200%`，三电机合计丢包率约 `0.407%`。
- 逐关节低阻尼手动验证确认 `ID 1 -> FR_hip (sign=+1)`、`ID 5 -> FR_thigh (sign=-1)`、`ID 9 -> FR_calf (sign=-1)`。
- ID 5/9 原始电机角与 URDF 角度变化方向相反，经方向系数转换后 `delta_urdf` 和 `dq_urdf` 与实机 URDF 运动方向一致；各目标电机持续 `crc=1`、`err=0`、`fails=0`并安全退出。
- 当前已完成 FL/FR 共6个关节的下标和方向实机验收；仍需验证 RL/RR 共6个关节、重新标定后的默认站立角和已知载荷下的力矩量纲。

### 2026-08-26 — Step 4 左后腿实机验收（部分完成）

- `/dev/ttyS7` 上 ID 2/6/10 以 500 Hz 联合轮询，丢包率分别为 `0.178%`、`0.311%`、`0.133%`，三电机合计丢包率约 `0.207%`。
- 逐关节低阻尼手动验证确认 `ID 2 -> RL_hip (sign=-1)`、`ID 6 -> RL_thigh (sign=+1)`、`ID 10 -> RL_calf (sign=+1)`。
- 位置 `delta_urdf` 和速度 `dq_urdf` 与实机 URDF 运动方向一致；各目标电机持续 `crc=1`、`err=0`、`fails=0`并安全退出。
- 本次手动测试中部分关节摆动范围较大；此结果只用于下标和符号验证，不作为机械行程或限位验收依据。
- 当前已完成 FL/FR/RL 共9个关节的下标和方向实机验收；仍需验证 RR 的 ID 3/7/11、重新标定后的默认站立角和已知载荷下的力矩量纲。

### 2026-08-26 — Step 4 右后腿实机验收（部分完成）

- `/dev/ttyS0` 上 ID 3/7/11 以 500 Hz 联合轮询时，丢包率约为 `34.2%`、`0.089%`、`5.111%`；ID 3 和 ID 11 通信未通过验收。
- 低阻尼手动验证确认 `ID 7 -> RR_thigh (sign=-1)` 和 `ID 11 -> RR_calf (sign=-1)`，有效反馈帧中 `delta_urdf/dq_urdf` 与实机 URDF 方向一致。
- ID 7/11 方向验证期间仍持续出现 CRC 警告，且 `all3` 曾恢复为 `OK`，说明故障 ID 3 仍在总线上偶尔响应；本次结果不能视为右后腿通信恢复。
- 当前完成 11/12 个关节的下标和方向实机验证；ID 3 必须等待新电机替换后重新完成通信及方向验收。
- 在 ID 3 替换且 ID 3/7/11 联合通信通过前，不得进行整机机械限位标定、站立或 NN 控制。

### 2026-08-13 — Step 5 实现

- IMU、电机反馈和估计输出全部使用 `CLOCK_MONOTONIC` 时间戳。
- 每个电机只在 CRC 正确且返回 ID 匹配时更新时间戳；失败时累计连续失败次数且不刷新旧时间。
- `EstimatedState` 记录 IMU 年龄、12 个电机反馈年龄、最大电机年龄、连续通信失败次数、估计 `dt` 和连续无效帧数。
- IMU 或任一电机反馈超过 100 ms、时间戳倒退、估计 `dt` 不在 1～100 ms、CRC/错误码/有限性异常时，整帧状态无效。
- 无效状态不进入 NN 推理或下发；连续 3 个无效状态触发统一急停。
- NN 增加独立状态流看门狗：即使估计线程完全停止发布，超过 100 ms 也会请求统一急停，防止总线持续发送旧指令。
- 纯软件测试覆盖电机陈旧反馈、时间倒退、异常 `dt`、连续失败计数及异常后恢复，全部通过。
- 后续系统集成测试：单电机反馈超时已实机验收；仍可补充 IMU 数据中断测试。

### 2026-08-26 — Step 5 实机验收（完成）

- 新增 `--monitor-only`：跳过机械标定和站立，12 电机从首个总线周期开始只发送阻尼，并强制 NN dry-run。
- 正常链路持续输出 `valid=1/status=0/bad_joint=-1`；IMU 年龄约 `1～2 ms`，本轮观测到的最大电机反馈年龄为 `38.11 ms`，低于 `100 ms` 阈值。
- 修正单次 CRC 丢包误判：状态估计继续使用最近有效反馈，只以反馈年龄、有限性和电机错误码决定有效性；偶发丢包仍单独累计。
- `Ctrl+C` 实机触发后，IMU、EST、NN 和 4 路总线线程均退出，12 电机阻尼锁存与资源回收完成；同时忽略 `SIGPIPE`，避免日志经 `tee` 时绕过清理。
- 使用 `--simulate-motor-timeout 0` 在真实硬件阻尼通信期间冻结 ID0 的估计输入时间戳；约 `140.068 ms` 后得到 `status=11/bad_joint=0/failures=7` 并自动安全退出，符合 `100 ms` 超时加约 `40 ms` 连续三帧确认的设计。
- 使用 `--simulate-imu-stream-loss` 停止向估计线程提交新 IMU 帧；NN 独立状态流看门狗在 `101 ms` 检出断流，随后完成全部线程回收和 12 电机阻尼退出。
- `s/S + Enter` 已改为 `poll()+read()` 直接读取，但本轮按用户选择以 `Ctrl+C` 作为人工急停验收方式，按键路径未单独实测。

### 2026-08-14 — Step 6 代码完善

- 新增独立 `PolicyObservationBuilder`，集中定义当前 48 维观测布局，避免 ONNX 推理函数内散落下标。
- `[36..47]` 首帧严格初始化为 12 个零 action，不再用 `default_pose` 冒充上一帧动作。
- 每次 Actor 推理后直接保存未经安全层改写的 12 维 raw action，下一帧 observation 使用该历史。
- `previous_raw_action` 与安全验证、平滑和电机下发使用的 `previous_accepted_command` 完全分离，安全层回退不改变训练语义。
- `ValidatingPolicy` 的平滑性参考使用最终接受命令；`ComparingPolicy` 将接受命令提交转发给主策略。
- 增加 action_scale、ONNX 单输入 `[1/-1,48]` 和单输出 `[1/-1,>=12]` 的初始化检查。
- 保留最近一次完整 observation 和 previous action，便于后续与训练端逐元素比较。
- 新增 `test_policy_observation`，覆盖 48 维下标、首帧零 action、目标反算以及 NaN 不污染历史，测试通过。
- 训练端定义已确认，布局为：机体系线速度 `[0..2]`、角速度 `[3..5]`、投影重力 `[6..8]`、速度命令 `[9..11]`、关节相对位置 `[12..23]`、关节速度 `[24..35]`、上一帧 raw action `[36..47]`。
- 20 帧真实训练 rollout 黄金向量回放通过：observation 最大绝对误差 `8.344650e-07`，ONNX raw action 最大绝对误差 `9.536743e-07`，关节目标最大绝对误差 `2.384186e-07`。
- 2026-08-26 增加运行时 CSV 的完整 48 维 observation 和 12 维 raw action 字段，以及 `validate_step6_log.py` 自动验收工具。
- 使用 `--monitor-only` 保持全部电机阻尼，在真实 IMU/关节反馈上采集运行时策略日志并通过自动校验。

### 2026-08-26 — Step 6 实机验收（完成）

- 使用实际部署模型 `creeper_flat_model_700_actor.onnx`，在 12 电机持续阻尼且 NN 强制 dry-run 的条件下记录 1483 帧。
- 全程状态估计保持 `valid=1/status=0/invalid_n=0/bad_joint=-1`；IMU 年龄约 `1～2 ms`，观测到的电机最大反馈年龄 `46.36 ms`，低于 `100 ms` 门限。
- 自动校验结果：机体系线速度、角速度、投影重力、零速度命令、关节速度及 previous raw-action 历史误差均为 0；关节相对位置最大误差 `1.2e-08`，目标公式最大误差 `6.5e-08`。
- 首轮运行发现反馈快照与 `now` 采样顺序竞态：新反馈时间可能短暂晚于预先采样的 `now`，使年龄保留为 `UINT64_MAX` 并误报超时；已改为取得全部电机快照后再读取 `CLOCK_MONOTONIC`，复测未再误触发。
- `Ctrl+C` 后 NN、IMU、EST 和总线线程全部退出，12 电机阻尼指令已发送，日志正常关闭。
- 结论：训练端/C++ observation、Actor raw-action 历史、ONNX 输出和关节目标公式均已验收一致，Step 6 完成。

### 2026-08-14 — Step 7 代码实现

- 新增 `CreeperLegKinematics`，使用 `Creeper20260714.urdf` 的 joint origin 实现 FL/FR/RL/RR 正运动学和 3×3 雅可比。
- 显式使用 motor-ID 排列：每条腿的关节为 `[leg, 4+leg, 8+leg]`，不依赖 URDF 文件的腿优先出现顺序。
- 新增 `LeggedOdometry`，以每足独立 `AIR→TOUCHDOWN→STANCE` 状态机联合关节力矩、足端速度和多帧迟滞输出接触与置信度。
- 足端相对高度只作为低权重置信度，不再是接触硬门槛；已增加超过 3.5 cm 高度带仍能承载接触的崎岖地形回归测试。
- 对支撑腿使用足端零世界速度约束反推机体速度，与 JY901S 比力积分做互补校正。
- 增加机体高度、支撑速度置信度、腾空、打滑和落地冲击诊断字段；无支撑足时置信度强制为 0。
- 新增 `test_legged_odometry` 纯软件测试，覆盖雅可比有限差分、静止零速、+X 平移方向、多足一致性和无接触降置信度，全部通过。
- 主程序 `robot_control` 编译通过；本次未打开 UART/GPIO，未运行任何电机程序。
- 待实机验收：在可靠支撑下记录四腿力矩基线，标定 contact on/off 阈值、折叠/站立高度带、打滑残差及落地冲击阈值。

### 2026-08-26 — Step 7 实机诊断准备

- 新增 `--step7-diagnostics`：正常执行机械限位标定和缓慢站立，但强制 NN dry-run，禁止同时使用 ONNX 或 `--monitor-only`。
- 以 2 Hz 输出 FL/FR/RL/RR 的接触状态、置信度、三关节力矩范数、足端高度、足端速度，以及机体世界/机体系速度、速度置信度、机体高度、腾空、打滑和冲击状态。
- 纯软件回归 `test_legged_odometry`、`test_state_estimator`、`test_linear_kf_estimator`、`test_linear_kf_adapter` 全部通过。
- 实机验收按平地静止基线、单腿卸载、缓慢平移/倾斜、打滑降置信度顺序逐项进行，当前等待第一阶段静止基线数据。

### 2026-08-27 — Step 7 接触迟滞初次实机标定

- 平地静止时足端高度稳定在 `0.27～0.28 m`，足端速度通常不超过 `0.04 m/s`，机体速度接近零，且 `slip=0/impact=0/odom_valid=1`。
- 旧阈值下 62 帧静止数据中 49 帧为 `1111`，随后 FL 力矩降到 `0.56～0.57 N·m` 时出现 13 帧 `0111`，确认 `contact_torque_off=0.6 N·m` 会误判轻载支撑足。
- 吊带承载→完全离地→重新落地测试确认：完全离地约 `0.09～0.23 N·m`，静态承载最低约 `0.53～0.56 N·m`；旧 `contact_torque_on=1.0 N·m` 导致重新落地后保持 `0000`，只有额外按压产生峰值才恢复个别触地。
- 统一迟滞阈值调整为 `contact_torque_on=0.45 N·m`、`contact_torque_off=0.30 N·m`，保留连续 3 帧触地和 2 帧离地确认。
- 新增低载荷“触地→离地→重新触地”纯软件回归；`test_legged_odometry`、`test_state_estimator` 和 `test_linear_kf_adapter` 全部通过。
- 首轮阈值复测已能完成 `1111→0000→1111`，但不平地面轻载 FR 在约 `0.25 N·m` 时仍会长期变成 `1011`；完全离地稳定值主要为 `0.09～0.18 N·m`，运动瞬态最高约 `0.23 N·m`。
- 第二轮统一迟滞阈值调整为 `contact_torque_on=0.28 N·m`、`contact_torque_off=0.20 N·m`，并增加约 `0.26 N·m` 轻载接触持续保持回归。
- 复测确认：不平地面稳定保持 `1111`；FL/FR/RL/RR 单腿及左右/前后两腿卸载时，对应接触位正确变为 0 并能恢复。
- 机体前/后/左/右缓慢倾移时，`vel_b.x/vel_b.y` 方向与机体坐标系一致，停止后回到约 `0～0.01 m/s`，过程保持 `odom_valid=1`。
- 完全吊起时稳定输出 `contact=0000/conf=0/vel_conf=0/airborne=1`，不伪装高置信速度。
- `--simulate-leg-slip FL` 在不改变电机指令的条件下实机验收：FL `foot_speed≈1.15～1.18 m/s`，持续输出 `contact=1111/slip=1/vel_conf=0.20/odom_valid=1`。
- 结论：Step 7 所有验收项完成；诊断故障注入始终强制 NN dry-run，未下发模拟速度到真实电机。

### 2026-08-27 — ONNX 静态站立平滑接管

- 首轮 ONNX 站立 dry-run 共记录 1064 帧，Actor 原始目标相对默认站姿的最大偏差约 `0.242 rad`；由于原安全层单帧跳变门限为 `0.10 rad`，每帧均触发 `JUMP` 并持续回退到 `StandingPolicy`。
- 新增 `SmoothTakeoverPolicy`：开环站立结束后，以已锁存的默认站立指令为起点，在 50 Hz 下使用 100 个已接受帧（默认约 2 秒）进行 smoothstep 混合，再完全交给 ONNX Actor。
- 策略链调整为 `ONNX → 可选 Compare → SmoothTakeover → 可选 Validate`，因此平滑后的每一帧仍必须通过关节限位、有限值、增益和 `0.10 rad` 跳变检查。
- 只有外层安全验证最终接受候选命令时才推进接管进度；维持旧帧、回退站姿或 Actor 推理失败均不会跳过过渡帧。
- 平滑只作用于电机候选目标和增益，不修改 Actor 原始输出，也不改变下一帧 observation 使用的 `previous_raw_action`，训练端语义保持不变。
- 新增纯软件回归 `test_smooth_takeover`：模拟最大 `0.24 rad` 初始目标差时，100 帧最大单帧变化为 `0.003600 rad`，全部通过安全验证；拒绝帧不推进进度。
- 使用首轮 1064 帧真实 Actor 日志离线重放，平滑后验证失败数为 0，整个日志最大相邻帧变化为 `0.005651 rad`。
- 尚待下一轮实机 dry-run 确认终端出现 `NN 平滑接管已启用: 2s / 100 帧` 和 `NN 平滑接管完成`，且不再持续出现 `Validation: FAIL | JUMP`；通过前不得去掉 `--dry-run`。

### 2026-08-27 — 机身高度定义修正

- 仿真连续零速度测试确认 `model_700.pt` 主动形成稳定蹲伏姿态：root link 高度约 `0.23513 m`，比默认站姿低约 `59.4 mm`。
- 同一URDF运动学下，仿真和实机稳定关节反馈对应的“base 到足球心”平均距离分别约为 `0.21132 m` 和 `0.21036 m`，仅相差约 `0.96 mm`，说明实机蹲伏高度与策略仿真基本一致。
- 原 `body_height` 只累计 `-foot_position_world.z`，实际表示 base 到足球心的距离；URDF 的四个 foot 使用半径 `0.020 m` 的球形碰撞体，因此与平地 root link 高度相比少计了足球半径。
- `LeggedOdometryConfig` 新增 `foot_radius=0.020 m`，`body_height`/`position.z` 现在表示 base 原点到足底接触平面的高度。
- 纯软件回归确认高度精确增加 `0.020 m`，接触状态、足端位置、速度估计和48维Actor observation不变；状态估计、Linear KF适配器及20帧黄金向量测试全部通过。

---

# 从零速NN接管到低速行走的推进计划

## 当前里程碑

- [x] 上电与12关节机械限位标定
- [x] 开环平滑站立到训练默认关节位置
- [x] 2秒/100有效帧的NN平滑接管
- [x] 零速度NN真实接管并稳定保持蹲伏姿态
- [x] 仿真/实机稳定蹲伏高度与关节姿态对比
- [x] `body_height`加入URDF足球半径，统一为base原点到足底接触平面的高度
- [ ] 满足低速行走前的全部安全门槛
- [ ] 完成平地低速前进、侧移和偏航实机验收

## Phase 1：电机通信恢复与长时间验收（当前优先级最高）

### 已知问题

- 最近一次真实NN接管运行约48.6秒后，ID2反馈年龄达到约`144.38 ms`，状态码变为`11`并触发统一安全停机。
- ID3此前存在明显高丢包率，仍需更换新电机并重新验收。

### 工作内容

- [x] 单独复测ID2。
- [x] 已执行左后腿总线ID2/6/10联合通信测试；ID10未达到丢包率门槛，需隔离复测并在修复后重新联合验收。
- [ ] 更换ID3后，复测ID3并复测右后腿总线ID3/7/11联合通信。
- [x] 已执行12电机以500 Hz阻尼通信持续测试60秒；测试过程未出现超过100 ms的反馈断流，但ID3仍为已知严重通信异常，需更换后重新验收。
- [ ] 零速度NN站立持续测试至少120秒。
- [x] 已记录每个电机的成功帧、CRC/短帧/超时次数、丢包率、最大反馈年龄和最长连续失败次数，并补充总线线程时序诊断。

### 验收门槛

- 每个电机丢包率原则上低于`1%`；若超出，必须记录原因并重新评估。
- 不得出现电机错误码、非有限反馈或超过`100 ms`的反馈断流。
- 运行期间不得因通信问题触发自动停机；人工`Ctrl+C`后12电机必须进入`Kd=0.136`阻尼并完成线程回收。

### Phase 1实施记录

- 2026-08-27，ID2单电机以500 Hz阻尼通信测试约60秒：累计事务`29502`，成功`29327`，短帧`166`，CRC/ID失败`9`，总丢包率`0.593%`。
- 总线频率除启动采样外保持约500 Hz；1秒窗口最差丢包率为`3.593%`，后半程累计丢包率逐步下降。
- stderr警告文件为空，程序正常输出`[SAFE]`并完成阻尼退出；ID2单电机累计指标通过，但联合测试仍需重点观察突发连续断流。
- 为后续联合测试补充`max_age_ms`和`max_streak`统计，分别记录最大反馈年龄和最长连续失败次数。
- 2026-08-27，左后腿ID2/6/10以500 Hz联合阻尼通信测试约60秒，每个电机累计事务`29502`；ID2成功`29459`、丢包`43`（`0.146%`，短帧`41`、协议/CRC-ID失败`2`），ID6成功`29495`、丢包`7`（`0.024%`，短帧`7`），两者通过累计丢包率门槛。
- 同次测试ID10成功`29004`、丢包`498`（`1.688%`，短帧`18`、协议/CRC-ID失败`480`），未达到低于`1%`的门槛；其最差1秒窗口为`4.208%`，应先做ID10单电机隔离测试，再判断是电机/支路线束问题还是三电机联合通信交互问题。
- 三个电机记录的`max_age_ms`均为`42.46 ms`，最长连续失败为ID2/ID6各`1`次、ID10为`2`次，未出现超过`100 ms`的反馈断流；程序正常输出`[SAFE]`且stderr警告文件为空。此前真实NN运行中ID2约`144.38 ms`的断流在本轮未复现，当前左后腿总线最明显的异常转为ID10。
- 2026-08-27，12电机四总线以500 Hz并发阻尼通信测试60秒并正常`[SAFE]`退出。最终累计丢包率：ID0 `0.193%`、ID1 `0.203%`、ID2 `0.207%`、ID3 `40.642%`、ID4 `0.271%`、ID5 `0.275%`、ID6 `0.190%`、ID7 `0.163%`、ID8 `0.193%`、ID9 `0.383%`、ID10 `4.335%`、ID11 `1.268%`。
- 本轮各电机最大反馈年龄为`38.12~63.04 ms`，均未越过`100 ms`；ID3虽累计丢包最高，但最长连续失败仅`10`帧且仍不断有成功帧刷新时间戳，所以没有触发超时。ID0/1/2等低累计丢包电机在真实控制中触发超时，更可能是突发整总线/线程调度停顿，而非累计丢包率造成。
- 此前停机日志只输出按ID顺序找到的第一个无效关节；ID0/1/2/3分别又是四条腿总线的首个ID，因此`bad_joint`容易被误解为单电机根因。
- 2026-08-27完成只读诊断增强：故障和健康日志输出全部无效关节的年龄/连续失败次数；每条总线无锁统计`max_loop_gap`、`max_cycle_duration`以及循环间隔超过`2/10/50/100 ms`的次数；故障触发和安全退出时均打印A~D四路快照。该修改不改变500 Hz收发、100 ms反馈阈值、NN连续三帧失效策略或电机指令。
- 完整工程编译通过；`test_nn_validation`、`test_state_estimator`、`test_legged_odometry`、`test_policy_observation`、`test_policy_golden_vectors`、`test_smooth_takeover`、`test_linear_kf_estimator`和`test_linear_kf_adapter`共8组纯软件回归测试全部通过。下一步用`monitor-only`分别在无ONNX和加载ONNX条件下运行至少120秒，对比总线线程调度间隔和通信循环耗时。
- 2026-08-27，第一组无ONNX的`monitor-only`对照测试运行约155秒，全程`EST valid=1/status=0`，未发生自动安全停机，健康输出观测到的最大电机反馈年龄为`58.92 ms`，人工退出后阻尼与线程回收正常。
- 总线时序结果：A `max_gap/max_cycle=41.31/41.28 ms`，B `61.14/61.09 ms`，C `61.36/61.31 ms`，D `41.32/41.22 ms`；四路均无循环间隔超过`100 ms`，B/C各有1次超过`50 ms`。`max_gap`几乎等于`max_cycle`，说明峰值主要发生在总线循环内部，而非循环之间的纯调度等待。
- `quietSerialRecv()`每次接收等待上限为`20 ms`，短帧会再等待一次；每路三电机又是串行事务。因此单次超时/短帧可在一个总线周期内累加到`40~120 ms`。ID3的大量失败主要是及时收到完整但CRC/ID错误的帧，不必等待20 ms超时，这解释了其累计丢包率极高却不必然最先触发100 ms反馈年龄保护。
- 2026-08-27，第二组加载`creeper_flat_model_700_actor.onnx`的`monitor-only`对照测试在约91.5秒时自动安全停机；失效快照为ID3 `145.69 ms/72`、ID7 `145.69 ms/72`、ID11 `147.43 ms/73`，确认右后腿整条D总线三个关节同时连续失去有效反馈，而不是日志仅误指向ID3。
- 故障时D总线仍为`499.9 Hz`，`max_gap/max_cycle=61.15/61.12 ms`，循环间隔超过`100 ms`次数为0；其余总线也无超过100 ms的循环间隔。因此本次不是ONNX导致总线线程停调，而是D总线在约72个500 Hz周期内持续快速收到无效/错位帧，导致三个成功反馈时间戳都没有刷新。
- 当前接收路径对完整但CRC错误或电机ID不匹配的帧只计数，不会立即清理RX残留或重新同步；ID3的已知高协议错误可能污染后续ID7/11事务并形成整条总线连续失败。下一步应在改变20 ms超时前，先增加逐电机事务耗时/失败类型快照，并评估在每次发送前清理陈旧RX数据、错误后立即重同步及限制单电机等待预算。

## Phase 2：NN蹲伏姿态下的四足接触检测

### 已知问题

- 最近一次NN蹲伏稳定段长期出现`contact=1101`，RL左后腿未稳定判定触地。
- RL三关节力矩范数约为：平均`0.263 N·m`、中位`0.219 N·m`、5%分位`0.125 N·m`，会频繁跨过当前`contact_torque_off=0.20 N·m`。

### 工作内容

- [ ] 在零速度NN蹲伏稳定后记录四腿力矩范数、足端速度、足端高度、接触状态和置信度。
- [ ] 分别抬起并放回FL/FR/RL/RR，采集真实触地与离地数据。
- [ ] 判断统一阈值是否仍适用；若不可分离，评估分腿阈值或基于雅可比/关节力矩的足端力估计。
- [ ] 保持崎岖地面不使用绝对足端高度作为触地硬门槛。

### 验收门槛

- 平地零速度蹲伏时四腿持续稳定为`1111`，不得长期固定缺失某条腿。
- 单腿抬起后对应位可靠变为0，放回承载后自动恢复为1。
- 左右、前后双腿卸载时，接触位与实机一致。
- 接触误判不得造成虚假高置信速度；无可靠支撑时必须降低速度置信度。

## Phase 3：修复控制前的数据并发与运行时安全缺口

### 工作内容

- [ ] 解决`PROJECT_ISSUES.md/ISSUE-004`：DoubleBuffer读指针可能在使用期间被生产者覆盖。
- [ ] 为IMU→EST和EST→NN链路增加并发压力测试或ThreadSanitizer测试。
- [ ] 统计IMU、EST、NN和四路总线线程的周期、最大延迟及deadline miss。
- [ ] 完善集中安全状态：通信超时、电机错误码、命令超时和线程超期统一锁存到安全退出。
- [ ] 如果协议能提供可靠温度/过流状态，将其接入安全状态；不能确认的字段不得猜测使用。

### 验收门槛

- 不存在数据竞争、读写撕裂或多读者破坏缓冲约定的问题。
- 任何无效或过期状态都不能进入新的NN推理和电机指令下发。
- 急停或安全故障触发后，旧的位置/速度/力矩命令不得再次覆盖阻尼指令。

## Phase 4：实现安全的三维速度命令源

### 前置确认

- [ ] 从训练端确认`vx/vy/yaw_rate`的训练范围、死区、静止判定阈值和命令刷新方式。

### 工作内容

- [ ] 默认命令始终为`[0,0,0]`。
- [ ] 增加运行时命令输入，首版可使用终端命令，后续再接遥控器/手柄。
- [ ] 对`vx/vy/yaw_rate`分别设置训练范围内的幅值限制。
- [ ] 增加斜率限制，禁止速度命令单帧跳变。
- [ ] 增加命令看门狗：输入超时先自动回零，严重故障进入统一阻尼退出。
- [ ] 保留`Ctrl+C`最高优先级急停，并提供显式零速度命令。
- [ ] 日志记录原始命令、限幅/斜坡后的实际命令和命令年龄。

### 验收门槛

- NaN/Inf、越界命令和过期命令均不能进入Actor observation。
- 命令源断开后能在规定时间内平滑回零。
- 急停不依赖命令线程正常工作。

## Phase 5：三维速度命令dry-run

### 测试顺序

- [ ] 零命令稳定运行。
- [ ] 小幅正/负`vx`单轴命令。
- [ ] 小幅正/负`vy`单轴命令。
- [ ] 小幅正/负`yaw_rate`单轴命令。
- [ ] 命令主动回零及命令源超时回零。

初始测试幅值必须根据训练端范围确定，不在确认前凭经验写死。

### 验收门槛

- observation `[9..11]`与最终实际命令逐元素一致。
- Actor输出无NaN/Inf、关节越界或持续安全回退。
- 命令变化和关节目标变化连续，单帧变化满足安全门限。
- 命令回零后Actor恢复稳定零速蹲伏策略。

## Phase 6：吊带保护下的首次低速行走

### 测试顺序

1. 零速NN接管并稳定至少5秒。
2. 仅给短时低幅前进命令，持续约1～2秒后回零。
3. 验证停止过程后，再依次测试后退、侧移和偏航。
4. 单轴命令全部通过后，才允许测试组合命令。

### 每轮测试必须满足

- [ ] 使用吊带防坠，四脚能够正常承载，腿部运动范围内无人和障碍物。
- [ ] 操作者全程准备按`Ctrl+C`，出现抖动、快速倾倒、滑移、异响或异常电流立即停止。
- [ ] 全程无验证回退、状态超时、电机错误码或持续通信异常。
- [ ] 接触切换与实际支撑腿一致，速度估计方向正确且停止后回到接近零。
- [ ] 回零后机器人重新稳定站立；`Ctrl+C`后正确进入12电机阻尼。

## Phase 7：长期行走、估计器对比与复杂地面

- [ ] 在同一实机日志上对比Complementary与Linear KF输出，先旁路观察，不直接切换控制。
- [ ] 平地单轴低速行走逐步延长到1分钟、5分钟和10分钟。
- [ ] 监控通信反馈年龄、推理延迟、线程deadline、电机错误和温升。
- [ ] 完成启动、运行、命令断开、人工急停和传感器/电机反馈故障的整套安全回归。
- [ ] 平地组合命令稳定后，再进入低矮障碍、缓坡和不平地面。

## 推进纪律

- 每个Phase必须依次完成“代码/配置确认→纯软件测试→dry-run→受保护实机测试→记录结果”。
- 前一Phase存在P0故障或未满足验收门槛时，不进入下一Phase。
- 不自动运行任何会驱动真实电机的测试；实机命令由操作者确认环境安全后手动执行。
- 每完成一项，在本节勾选并记录日期、日志路径、关键统计和遗留问题。
